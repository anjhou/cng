class Utility{static flashButton(id){const b=document.getElementById(id);if(!b)return;b.classList.remove("button-success");void b.offsetWidth;b.classList.add("button-success")}static flashError(id){const b=document.getElementById(id);if(!b)return;b.classList.remove("button-error");void b.offsetWidth;b.classList.add("button-error")}static setLoading(id,t="Processing..."){const b=document.getElementById(id);if(!b)return;b.dataset.originalText=b.textContent;b.textContent=t;b.disabled=true}static clearLoading(id){const b=document.getElementById(id);if(!b)return;b.textContent=b.dataset.originalText||b.textContent;b.disabled=false}}